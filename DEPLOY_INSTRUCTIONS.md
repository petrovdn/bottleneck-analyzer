# 🚀 Инструкция по развертыванию в интернете

## Быстрый старт (5 минут)

### Вариант 1: Vercel (Рекомендуется - самый простой)

#### Шаг 1: Подготовка GitHub репозитория

```bash
cd business-bottleneck-analyzer

# Если еще нет Git репозитория
git init
git add .
git commit -m "Initial commit"

# Создайте репозиторий на github.com, затем:
git remote add origin https://github.com/ВАШ_USERNAME/bottleneck-analyzer.git
git push -u origin main
```

#### Шаг 2: Деплой на Vercel

1. **Зайдите на [vercel.com](https://vercel.com)**
   - Нажмите "Sign Up"
   - Войдите через GitHub

2. **Создайте проект:**
   - Нажмите "Add New Project"
   - Выберите ваш репозиторий `bottleneck-analyzer`
   - Vercel автоматически определит Next.js

3. **Добавьте переменную окружения:**
   - В разделе "Environment Variables"
   - Имя: `OPENAI_API_KEY`
   - Значение: ваш ключ OpenAI (получите на https://platform.openai.com/api-keys)
   - Выберите все окружения (Production, Preview, Development)
   - Нажмите "Add"

4. **Деплой:**
   - Нажмите "Deploy"
   - Подождите 2-3 минуты
   - **Готово!** 🎉

#### Шаг 3: Получите ссылку

После деплоя вы получите ссылку:
```
https://your-project.vercel.app
```

Эту ссылку можно показывать клиентам и коллегам!

---

### Вариант 2: Railway (Альтернатива)

1. Зайдите на [railway.app](https://railway.app)
2. "Start a New Project" → "Deploy from GitHub repo"
3. Выберите репозиторий
4. Добавьте переменную `OPENAI_API_KEY` в Variables
5. Готово!

---

## Что нужно для деплоя

### Обязательно:
- ✅ GitHub аккаунт
- ✅ OpenAI API ключ (https://platform.openai.com/api-keys)
- ✅ 5-10 минут времени

### Опционально:
- Свой домен (можно настроить в Vercel/Railway)

---

## Проверка после деплоя

После успешного деплоя проверьте:

1. ✅ Главная страница открывается
2. ✅ Форма Discovery работает
3. ✅ Можно заполнить данные о бизнесе
4. ✅ Мультиагентная система инициализируется
5. ✅ Диалог с системой работает
6. ✅ Узкие места находятся и отображаются

---

## Обновление приложения

После изменений в коде:

```bash
git add .
git commit -m "Update"
git push
```

Vercel/Railway автоматически задеплоит новую версию!

---

## Стоимость

- **Vercel**: Бесплатно (до 100GB трафика/месяц)
- **OpenAI API**: ~$0.20-0.30 за сессию анализа
- **Итого для демонстрации**: ~$0-50/месяц

---

## Безопасность

⚠️ **Важно:**
- Никогда не коммитьте `.env.local` в Git
- Используйте переменные окружения на платформе деплоя
- Ограничьте доступ к API ключу OpenAI

---

## Troubleshooting

### Ошибка: "OPENAI_API_KEY is not defined"
→ Проверьте, что переменная добавлена в Vercel/Railway и передеплойте

### Ошибка при сборке
→ Проверьте логи в Dashboard → Deployments

### Приложение не работает
→ Проверьте, что API ключ OpenAI активен и имеет баланс

---

**Готово!** Теперь у вас есть публичная ссылка для демонстрации системы 🎉

Подробная документация: см. `DEPLOY.md`

