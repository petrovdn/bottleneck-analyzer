# 📦 Инструкция по установке

Полное пошаговое руководство по установке и запуску приложения.

## Системные требования

### Обязательные:
- **Node.js**: версия 18.0.0 или выше
- **npm**: версия 9.0.0 или выше (обычно идет с Node.js)
- **Операционная система**: macOS, Windows, Linux
- **Браузер**: Chrome, Firefox, Safari или Edge (последние версии)
- **Интернет**: Стабильное подключение для работы с OpenAI API

### Проверка установленных версий:

```bash
node --version
# Должно показать v18.x.x или выше

npm --version
# Должно показать 9.x.x или выше
```

### Если Node.js не установлен:

**macOS:**
```bash
# Используя Homebrew
brew install node
```

**Windows:**
Скачайте установщик с [nodejs.org](https://nodejs.org/)

**Linux:**
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install nodejs npm

# Или используя nvm (рекомендуется)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install 18
nvm use 18
```

## Шаг 1: Получение проекта

Проект уже создан в директории:
```
/Users/Dima/langchain-project/business-bottleneck-analyzer/
```

Перейдите в директорию:
```bash
cd /Users/Dima/langchain-project/business-bottleneck-analyzer
```

## Шаг 2: Установка зависимостей

Установите все необходимые пакеты:

```bash
npm install
```

Это установит:
- **Next.js 14** - фреймворк для React приложений
- **React 18** - библиотека для UI
- **TypeScript** - типизация
- **TailwindCSS** - стилизация
- **LangChain** - фреймворк для мультиагентов
- **OpenAI SDK** - работа с GPT-4
- **Zustand** - управление состоянием
- **Lucide React** - иконки

**Время установки:** 2-5 минут в зависимости от скорости интернета

### Возможные проблемы при установке:

#### Проблема: "EACCES: permission denied"

**Решение:**
```bash
sudo chown -R $(whoami) ~/.npm
npm install
```

#### Проблема: "unable to resolve dependency tree"

**Решение:**
```bash
npm install --legacy-peer-deps
```

#### Проблема: Медленная установка

**Решение:** Используйте быстрый зеркало npm:
```bash
npm install --registry=https://registry.npmjs.org/
```

## Шаг 3: Настройка переменных окружения

### 3.1 Создайте файл .env.local

```bash
touch .env.local
```

### 3.2 Добавьте OpenAI API ключ

Откройте `.env.local` в любом текстовом редакторе и добавьте:

```env
OPENAI_API_KEY=sk-proj-ваш-ключ-здесь
```

**Где взять ключ?**

1. Зарегистрируйтесь на [platform.openai.com](https://platform.openai.com/signup)
2. Пополните баланс минимум на $5
3. Создайте API ключ в [API Keys](https://platform.openai.com/api-keys)
4. Скопируйте ключ

📖 **Подробная инструкция:** См. [ENV_SETUP.md](./ENV_SETUP.md)

### 3.3 Проверьте файл .gitignore

Убедитесь что `.env.local` добавлен в `.gitignore`:

```bash
cat .gitignore | grep .env
# Должно показать: .env*.local
```

## Шаг 4: Запуск приложения

### 4.1 Запустите dev сервер

```bash
npm run dev
```

Вы должны увидеть:
```
▲ Next.js 14.0.0
- Local:        http://localhost:3000
- Environments: .env.local

✓ Ready in 2.5s
```

### 4.2 Откройте в браузере

Перейдите на [http://localhost:3000](http://localhost:3000)

Вы должны увидеть экран Discovery с формой.

### 4.3 Проверьте работоспособность

1. Заполните форму тестовыми данными
2. Нажмите "Далее: анализ узких мест"
3. Подождите 10-30 секунд (идет анализ через GPT-4)
4. Вы должны увидеть список найденных узких мест

✅ Если все работает - установка успешна!

## Структура проекта после установки

```
business-bottleneck-analyzer/
├── node_modules/          # Установленные зависимости (>100MB)
├── src/                   # Исходный код
│   ├── app/              # Next.js App Router
│   │   ├── api/          # API endpoints
│   │   ├── globals.css   # Стили
│   │   ├── layout.tsx    # Layout
│   │   └── page.tsx      # Главная страница
│   ├── components/       # React компоненты
│   ├── lib/              # Библиотеки и агенты
│   ├── store/            # Zustand store
│   └── types/            # TypeScript типы
├── .env.local            # Переменные окружения (создается вами)
├── .gitignore            # Игнорируемые файлы
├── package.json          # Зависимости проекта
├── tsconfig.json         # TypeScript конфигурация
├── tailwind.config.ts    # TailwindCSS конфигурация
├── next.config.js        # Next.js конфигурация
└── README.md             # Основная документация
```

## Полезные команды

### Разработка

```bash
# Запустить dev сервер
npm run dev

# Запустить на другом порту
PORT=3001 npm run dev

# Очистить кэш и перезапустить
rm -rf .next && npm run dev
```

### Production

```bash
# Собрать для production
npm run build

# Запустить production сборку
npm start

# Проверить типы TypeScript
npx tsc --noEmit
```

### Отладка

```bash
# Проверить линтер
npm run lint

# Посмотреть версии пакетов
npm list --depth=0

# Проверить устаревшие пакеты
npm outdated

# Обновить зависимости
npm update
```

### Очистка

```bash
# Удалить node_modules и переустановить
rm -rf node_modules package-lock.json
npm install

# Очистить кэш Next.js
rm -rf .next

# Очистить кэш npm
npm cache clean --force
```

## Тестирование установки

Создайте файл `test-installation.js` в корне проекта:

```javascript
const fs = require('fs');
const path = require('path');

console.log('🔍 Проверка установки...\n');

// Проверка Node.js версии
const nodeVersion = process.version;
console.log(`✓ Node.js: ${nodeVersion}`);

// Проверка наличия важных файлов
const requiredFiles = [
  'package.json',
  'tsconfig.json',
  'next.config.js',
  '.gitignore',
  'src/app/page.tsx',
  'src/store/useAppStore.ts',
];

requiredFiles.forEach(file => {
  if (fs.existsSync(path.join(__dirname, file))) {
    console.log(`✓ ${file} существует`);
  } else {
    console.log(`✗ ${file} не найден!`);
  }
});

// Проверка .env.local
if (fs.existsSync(path.join(__dirname, '.env.local'))) {
  console.log('✓ .env.local существует');
  
  const envContent = fs.readFileSync(path.join(__dirname, '.env.local'), 'utf8');
  if (envContent.includes('OPENAI_API_KEY=sk-')) {
    console.log('✓ OPENAI_API_KEY настроен');
  } else {
    console.log('⚠ OPENAI_API_KEY не настроен');
  }
} else {
  console.log('⚠ .env.local не найден - создайте его!');
}

// Проверка node_modules
if (fs.existsSync(path.join(__dirname, 'node_modules'))) {
  console.log('✓ node_modules установлены');
} else {
  console.log('✗ node_modules не найдены - выполните npm install');
}

console.log('\n✅ Проверка завершена!');
```

Запустите:
```bash
node test-installation.js
```

## Что делать после установки?

1. 📖 **Прочитайте документацию:**
   - [README.md](./README.md) - общий обзор
   - [QUICKSTART.md](./QUICKSTART.md) - быстрый старт
   - [EXAMPLES.md](./EXAMPLES.md) - примеры использования
   - [ARCHITECTURE.md](./ARCHITECTURE.md) - архитектура

2. 🧪 **Протестируйте приложение:**
   - Попробуйте примеры из EXAMPLES.md
   - Проверьте все три экрана
   - Сгенерируйте промпт

3. 🎨 **Кастомизируйте:**
   - Измените цвета в `tailwind.config.ts`
   - Добавьте свой логотип
   - Адаптируйте промпты под свою специфику

4. 🚀 **Разверните:**
   - Деплой на Vercel (рекомендуется)
   - Или на любую другую платформу

## Удаление приложения

Если нужно полностью удалить:

```bash
cd /Users/Dima/langchain-project
rm -rf business-bottleneck-analyzer
```

Также удалите API ключ в [OpenAI dashboard](https://platform.openai.com/api-keys)

## Поддержка

### Получение помощи:

1. **Документация:**
   - Проверьте README.md и другие .md файлы
   - Ищите по ключевым словам

2. **Логи:**
   - Проверьте терминал где запущен `npm run dev`
   - Откройте консоль браузера (F12)

3. **Сообщество:**
   - Создайте Issue в репозитории
   - Опишите проблему детально
   - Приложите скриншоты и логи

### Частые вопросы:

**Q: Можно ли использовать бесплатно?**
A: Приложение бесплатное, но требует OpenAI API ключ. OpenAI берет оплату за использование GPT-4 (~$0.20 за сессию).

**Q: Какая версия GPT используется?**
A: По умолчанию GPT-4 Turbo. Можно изменить в коде.

**Q: Безопасно ли вводить данные о бизнесе?**
A: Данные отправляются только в OpenAI API. Убедитесь что соблюдаете политику конфиденциальности вашей компании.

**Q: Можно ли использовать другие LLM (Anthropic, Google)?**
A: Да, но потребуется модификация кода в `src/lib/agents/`.

---

🎉 **Готово!** Наслаждайтесь использованием анализатора узких мест!

