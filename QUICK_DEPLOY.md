# 🚀 Быстрый деплой за 5 минут

## Самый простой способ (Vercel)

### Шаг 1: Подготовка (2 минуты)

1. **Создайте GitHub репозиторий** (если еще нет):
```bash
cd business-bottleneck-analyzer
git init
git add .
git commit -m "Initial commit"
# Создайте репозиторий на github.com и выполните:
git remote add origin https://github.com/ВАШ_USERNAME/business-bottleneck-analyzer.git
git push -u origin main
```

2. **Получите OpenAI API ключ**:
   - Зайдите на https://platform.openai.com/api-keys
   - Создайте новый ключ
   - Скопируйте его (он понадобится на шаге 3)

### Шаг 2: Деплой на Vercel (3 минуты)

1. **Зайдите на [vercel.com](https://vercel.com)**
   - Нажмите "Sign Up"
   - Войдите через GitHub

2. **Создайте проект**:
   - Нажмите "Add New Project"
   - Выберите ваш репозиторий `business-bottleneck-analyzer`
   - Vercel автоматически определит Next.js

3. **Добавьте переменную окружения**:
   - В разделе "Environment Variables"
   - Имя: `OPENAI_API_KEY`
   - Значение: вставьте ваш ключ OpenAI
   - Выберите все окружения (Production, Preview, Development)
   - Нажмите "Add"

4. **Деплой**:
   - Нажмите "Deploy"
   - Подождите 2-3 минуты
   - Готово! 🎉

### Шаг 3: Проверка

После деплоя вы получите ссылку типа:
```
https://business-bottleneck-analyzer-xxxxx.vercel.app
```

Откройте ссылку и проверьте:
- ✅ Форма открывается
- ✅ Можно заполнить данные
- ✅ Мультиагентная система работает

## Что дальше?

### Настройка домена (опционально)

1. В Vercel Dashboard → Settings → Domains
2. Добавьте свой домен (например, `bottleneck-analyzer.com`)
3. Настройте DNS по инструкции Vercel

### Обновление приложения

Просто закоммитьте изменения в Git:
```bash
git add .
git commit -m "Update"
git push
```

Vercel автоматически задеплоит новую версию!

## Альтернатива: Railway (еще проще)

1. Зайдите на [railway.app](https://railway.app)
2. "Start a New Project" → "Deploy from GitHub repo"
3. Выберите репозиторий
4. Добавьте `OPENAI_API_KEY` в Variables
5. Готово!

## Проблемы?

### Ошибка: "OPENAI_API_KEY is not defined"
→ Проверьте, что переменная добавлена в Vercel/Railway и передеплойте

### Ошибка при сборке
→ Проверьте логи в Dashboard → Deployments

### Приложение не работает
→ Проверьте, что API ключ OpenAI активен и имеет баланс

---

**Готово!** Теперь у вас есть публичная ссылка для демонстрации 🎉

