# 🔐 Настройка переменных окружения

Подробная инструкция по настройке API ключей и переменных окружения.

## Требуемые переменные

### OPENAI_API_KEY (Обязательно)

OpenAI API ключ используется для работы мультиагентной системы.

## Шаг 1: Получение OpenAI API ключа

### 1.1 Создайте аккаунт на OpenAI

Перейдите на [https://platform.openai.com/signup](https://platform.openai.com/signup) и зарегистрируйтесь.

### 1.2 Пополните баланс

1. Перейдите в [Billing](https://platform.openai.com/account/billing)
2. Нажмите "Add payment method"
3. Добавьте способ оплаты
4. Пополните баланс минимум на $5

💡 **Примерная стоимость использования:**
- Один анализ бизнеса: ~$0.10 - $0.20
- Генерация одного промпта: ~$0.15 - $0.30
- С балансом $5 можно провести ~20-30 полных сессий

### 1.3 Создайте API ключ

1. Перейдите в [API Keys](https://platform.openai.com/api-keys)
2. Нажмите "Create new secret key"
3. Дайте ключу понятное имя (например, "business-bottleneck-analyzer")
4. Скопируйте ключ (он показывается только один раз!)

⚠️ **Важно**: Сохраните ключ в надежном месте. Если потеряете, придется создавать новый.

## Шаг 2: Настройка в проекте

### Вариант 1: Использование .env.local (Рекомендуется)

Создайте файл `.env.local` в корне проекта:

```bash
cd business-bottleneck-analyzer
touch .env.local
```

Откройте файл в редакторе и добавьте:

```env
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

**Преимущества:**
- Файл автоматически игнорируется Git
- Переменные доступны только в локальной разработке
- Безопасно

### Вариант 2: Использование .env (Альтернатива)

Если `.env.local` не работает, создайте `.env`:

```bash
touch .env
```

И добавьте тот же контент:

```env
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

⚠️ **Важно**: Убедитесь, что `.env` добавлен в `.gitignore`

## Шаг 3: Проверка настройки

### 3.1 Перезапустите dev сервер

Если сервер уже запущен, остановите его (Ctrl+C) и запустите снова:

```bash
npm run dev
```

### 3.2 Проверьте в браузере

1. Откройте [http://localhost:3000](http://localhost:3000)
2. Заполните форму Discovery
3. Нажмите "Далее: анализ узких мест"
4. Если API ключ настроен правильно, вы увидите результаты анализа

### 3.3 Проверьте в консоли

Если видите ошибку:
```
Error: OpenAI API key not found
```

Значит ключ не загрузился. Проверьте:
1. Файл `.env.local` находится в корне проекта
2. Ключ написан правильно: `OPENAI_API_KEY=sk-...`
3. Нет пробелов вокруг знака `=`
4. Сервер был перезапущен после создания файла

## Дополнительные переменные (Опционально)

### Настройка модели

По умолчанию используется `gpt-4-turbo-preview`. Вы можете изменить модель:

```env
OPENAI_API_KEY=sk-proj-xxxxxx
OPENAI_MODEL=gpt-4-turbo-preview
```

Доступные модели:
- `gpt-4-turbo-preview` (рекомендуется) - лучшее качество
- `gpt-4` - стабильная версия
- `gpt-3.5-turbo` - быстрее и дешевле, но хуже качество

### Настройка температуры

Контролирует креативность модели (0.0 - 1.0):

```env
OPENAI_API_KEY=sk-proj-xxxxxx
OPENAI_TEMPERATURE=0.7
```

- `0.0-0.3` - более предсказуемые ответы
- `0.4-0.7` - баланс (рекомендуется)
- `0.8-1.0` - более креативные ответы

### Настройка max tokens

Максимальное количество токенов в ответе:

```env
OPENAI_API_KEY=sk-proj-xxxxxx
OPENAI_MAX_TOKENS=4000
```

## Безопасность

### ✅ Хорошие практики

1. **Никогда не коммитьте API ключи в Git**
   ```bash
   # Проверьте .gitignore
   cat .gitignore | grep .env
   ```

2. **Используйте разные ключи для dev и production**
   - Dev ключ: для локальной разработки
   - Production ключ: для продакшена

3. **Ограничьте права ключа**
   - В OpenAI dashboard можно установить лимиты использования
   - Рекомендуется установить месячный лимит

4. **Регулярно ротируйте ключи**
   - Создавайте новый ключ каждые 3-6 месяцев
   - Удаляйте старые неиспользуемые ключи

5. **Мониторьте использование**
   - Проверяйте [Usage dashboard](https://platform.openai.com/usage)
   - Настройте email уведомления о превышении бюджета

### ❌ Чего НЕ делать

1. ❌ Не публикуйте ключи в публичных репозиториях
2. ❌ Не сохраняйте ключи в коде
3. ❌ Не передавайте ключи на клиент (frontend)
4. ❌ Не используйте один ключ для всех проектов

## Troubleshooting

### Ошибка: "Invalid API key"

**Причины:**
- Ключ скопирован неправильно (лишние пробелы)
- Ключ был удален в OpenAI dashboard
- Ключ не активирован

**Решение:**
1. Проверьте ключ в `.env.local`
2. Создайте новый ключ
3. Убедитесь что баланс OpenAI положительный

### Ошибка: "Rate limit exceeded"

**Причина:** Превышен лимит запросов к API

**Решение:**
1. Подождите несколько минут
2. Проверьте лимиты в [OpenAI dashboard](https://platform.openai.com/account/limits)
3. Увеличьте лимиты если нужно

### Ошибка: "Insufficient credits"

**Причина:** Закончился баланс

**Решение:**
1. Пополните баланс в [Billing](https://platform.openai.com/account/billing)
2. Настройте auto-recharge

### Переменные не загружаются

**Решение:**
1. Убедитесь что файл называется `.env.local` (с точкой в начале)
2. Файл должен быть в корне проекта
3. Перезапустите dev сервер
4. Проверьте что нет опечаток в названии переменной

## Production deployment

Для деплоя на Vercel:

1. Перейдите в настройки проекта на Vercel
2. Откройте вкладку "Environment Variables"
3. Добавьте `OPENAI_API_KEY` со значением вашего ключа
4. Выберите окружения: Production, Preview, Development
5. Сохраните и передеплойте

Для других платформ (AWS, Google Cloud, etc.) используйте их системы управления секретами.

---

🔐 **Помните**: API ключ - это как пароль. Храните его в безопасности!

